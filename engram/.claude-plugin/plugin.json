{
  "name": "engram",
  "version": "5.2.4",
  "description": "Persistent memory for Claude Code — static session-start context, durable memory tools, and compatibility hooks aligned with v5. api_token is optional: set via /config UI or via ENGRAM_AUTH_ADMIN_TOKEN env var in ~/.claude/settings.json (survives CC credential store races).",
  "author": {
    "name": "thebtf"
  },
  "homepage": "https://github.com/thebtf/engram",
  "repository": "https://github.com/thebtf/engram",
  "license": "MIT",
  "userConfig": {
    "server_url": {
      "title": "Server URL",
      "description": "Engram server URL (e.g. http://192.168.1.100:37777)",
      "type": "string",
      "required": true,
      "sensitive": false
    },
    "api_token": {
      "title": "API Token",
      "description": "API authentication token for your engram server. Optional — if unset here, the plugin falls back to the ENGRAM_AUTH_ADMIN_TOKEN env var (typically set in ~/.claude/settings.json). This workaround survives CC credential-store races that periodically wipe sensitive plugin values (see anthropics/claude-code#45551).",
      "type": "string",
      "required": false,
      "sensitive": true
    }
  }
}
